node_modules/
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo

# 비밀값 — .env.example 만 커밋한다.
# 하네스(scripts/execute.py)가 매 step 종료 시 `git add -A` 로 커밋하므로
# 이 항목이 없으면 토큰이 그대로 올라간다. 레포는 Giscus 때문에 public 이다.
.env
.env.*
!.env.example
.vercel

# phase execution outputs
phases/**/phase*-output.json
phases/**/step*-output.json
__pycache__
.pytest_cache/

# macOS
.DS_Store

# 브라우저 검증 산출물 (Playwright MCP)
.playwright-mcp/

# 작업 메모와 검증 도구 — 레포 산출물이 아니다. 세션 간에만 쓴다.
handoff*.md
.review-tools/
